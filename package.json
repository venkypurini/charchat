{
  "name": "charchat-root",
  "version": "1.0.0",
  "private": true,
  "description": "CharChat Full Stack Root",
  "scripts": {
    "build": "cd backend && npm install --include=dev && npm run build && cd ../frontend && npm install --include=dev && npm run build",
    "start": "node backend/dist/server.js"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
